services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./todo.db:/app/todo.db
    environment:
      - FLASK_APP=main.py
      - FLASK_ENV=production
    restart: unless-stopped

